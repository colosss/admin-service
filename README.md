
# 🔐 Auth Service

**Auth Service** — микросервис аутентификации и авторизации для платформы форума.
Он управляет регистрацией пользователей, входом в систему и проверкой прав доступа через JWT. Сервис взаимодействует с другими компонентами платформы через **gRPC** и события **Kafka**, без прямого HTTP-доступа извне.

Auth Service является частью проекта сайта **[Microservices_Site](https://github.com/Weit145/microservices_site)**. 




## 🎯 Основные функции

1. **Регистрация пользователей**

   * Создание учетных записей с проверкой корректности данных.
   * Создание JWT-токена для подверждения через почту.

2. **Аутентификация**

   * Вход по логину и паролю.
   * Генерация JWT-токенов доступа и обновления.

3. **Авторизация и проверка прав**

   * Контроль доступа к ресурсам на основе ролей.
   * Верификация JWT-токенов и сроков их действия.

4. **Внутреннее API**

   * gRPC-интерфейс для взаимодействия с Gateway, User Service и другими микросервисами.


## 🔧 Технологический стек и зависимости

| Компонент                            | Назначение                                       |
| ------------------------------------ | ------------------------------------------------ |
| **Python**                           | Основной язык разработки                         |
| **gRPC / grpcio / grpcio-tools**     | Межсервисное взаимодействие                      |
| **PostgreSQL**                       | Хранилище данных пользователей (`postgres_auth`) |
| **SQLAlchemy 2.x (async)**           | ORM для асинхронной работы с БД                  |
| **asyncpg**                          | Асинхронный драйвер PostgreSQL                   |
| **Alembic**                          | Миграции базы данных                             |
| **PyJWT**                            | Работа с JWT-токенами                            |
| **argon2-cffi / passlib**            | Хэширование паролей                              |
| **dotenv**                           | Работа с переменными окружения                   |
| **pydantic-settings**                | Конфигурация и валидация настроек                |
| **aiokafka**                         | Публикация/подписка на события Kafka             |
| **Poetry**                           | Управление зависимостями и окружением            |
| **Docker**                           | Контейнеризация и деплой                         |


## 🌐 Взаимодействие с другими сервисами

* **gRPC-клиенты**: API Gateway.
* **Kafka**: публикация и подписка на события.
* **Хранилище данных**: отдельная база `postgres_auth`.

## 🛡️ Безопасность

* Вся логика аутентификации и авторизации централизована.
* JWT является основным механизмом авторизации.
* gRPC-контракты описаны в каталоге `proto`.
* Пароли хранятся безопасно с использованием Argon2 через `argon2-cffi` и `passlib`.
* Интеграция с Kafka используется для событийной синхронизации с другими сервисами.
